import matplotlib.pyplot as plt
import os

path = '/content/catdog'
imgs = os.listdir(path)

for i in range(len(imgs)):
    plt.subplot(3, 3, i + 1)
    img = plt.imread(os.path.join(path, imgs[i]))
    plt.imshow(img)
    plt.axis('off')

plt.show()
